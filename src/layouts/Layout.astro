---
import '../styles/theme.css';
import '../styles/components.css';
import '../styles/prose.css';

interface Props {
	title?: string;
	description?: string;
}

const {
	title = 'Found in Space',
	description = 'An educational platform for exploring the Gaia-mapped solar neighbourhood.',
} = Astro.props;
const basePath = import.meta.env.BASE_URL;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" href={`${basePath}robbie.ico`} />
		<link rel="icon" type="image/png" href={`${basePath}robbie-32.png`} />
		<meta name="generator" content={Astro.generator} />
		<meta name="description" content={description} />
		<title>{title}</title>
	</head>
	<body>
		<p class="site-notice" role="status">
			content is still in production and may contain factual errors
		</p>
		<slot />
	</body>
</html>
